display_information:
  name: Ivy Tendril
  description: CLI notifications via Notify.Console

features:
  bot_user:
    display_name: Ivy Tendril
    always_online: false

oauth_config:
  scopes:
    bot:
      - incoming-webhook

settings:
  org_deploy_enabled: false
  socket_mode_enabled: false
